<h2> New User Activity </h2>

<%= form_for @user_activity do |f| %>
    <%= f.label :User %>
    <%= f.collection_select :user_id, User.all, :id, :name %>
    
    <%= f.label :Activity %>
    <%= f.collection_select :activity_id, Activity.all, :id, :name %>

    <%= f.submit %>
<% end %>

label is the text that appears in front of the drop down
user_id